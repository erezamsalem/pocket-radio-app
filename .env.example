This is an example file. Copy this to a new file named .env
and replace the placeholder values with your actual credentials.
API_KEY="YOUR_GOOGLE_GEMINI_API_KEY_HERE"


This file is now ready in your project directory `C:\MICROSOFT\pocket-radio-app`.

### 2. Add, Commit, and Push to GitHub

Now, let's add this new example file to your GitHub repository. Since it contains no secrets, it's completely safe to commit.

Run these commands one by one in your terminal:

1.  **Stage the new file:**
    ```powershell
    git add .env.example
    ```

2.  **Commit the file:**
    ```powershell
    git commit -m "Docs: Add .env.example for environment setup"
    ```

3.  **Push to GitHub:**
    ```powershell
    git push